{% extends sonata_block.templates.block_base %}
 
{% block block %}
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">Cache legen</h3>
        </div>
        <div class="box-body">
            <table class="table">
                <tbody>
                    <tr>
                        <td><i class="fa fa-exclamation-triangle" aria-hidden="true"></i> Voor de snelheid worden onder andere 'routes' gecached. Indien u aan de routes van het CMS wijzigingen heeft aangebracht kan het noodzakelijk zijn om de cache te legen.</td>
                        <td><a href="{{ path('admin_fbeen_simplecms_route_clear_cache') }}" class="btn btn-danger"><i class="fa fa-cogs" aria-hidden="true"></i> Cache legen</a></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}